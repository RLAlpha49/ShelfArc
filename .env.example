# Supabase Configuration
# Get these values from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
# Publishable key is safe to expose in the browser
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-supabase-publishable-key
# Alternative alias (accepted as fallback)
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
# Secret key must NEVER be exposed to the browser
SUPABASE_SECRET_KEY=your-supabase-secret-key
# Google Books API key for search
GOOGLE_BOOKS_API_KEY=your-google-books-api-key
# Optional: multiple keys (comma/space/semicolon/newline separated) for rotation
GOOGLE_BOOKS_API_KEYS=your-google-books-api-key-1,your-google-books-api-key-2
# Supabase storage bucket for uploaded media
SUPABASE_STORAGE_BUCKET=media

# Optional: enable distributed rate limiting in non-production environments
# (requires SUPABASE_SECRET_KEY and the `rate_limit_consume` function in Postgres)
DISTRIBUTED_RATE_LIMIT_ENABLED=false

# Backup settings (Supabase Edge Function secrets must be set in the Supabase dashboard, not in this .env file)
BACKUP_BUCKET=backups
BACKUP_PREFIX=db-backups
BACKUP_KEEP_LAST=7
BACKUP_MAX_TOTAL_MB=1024
BACKUP_PAGE_SIZE=1000
BACKUP_TABLES=
BACKUP_EXCLUDE_TABLES=
BACKUP_SECRET=your-backup-secret
